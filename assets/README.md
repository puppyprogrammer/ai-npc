# assets

The root README references `assets/hero.png`. Save the avatar screenshot here as **`hero.png`**
(then `git add assets/hero.png && git commit && git push`, or drag-drop it into the README on GitHub).

Ideal hero shot: the 3D AI NPC in a furnished scene — ~1200px wide, a GIF of it talking/walking is even better.
