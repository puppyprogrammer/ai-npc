# ai-npc demo

The hosted, interactive demo lives here — **the single most important asset for adoption.** A
visitor should land, talk to the NPC, watch it emote and find a seat, and immediately want to share it.

Planned: a small Vite app that mounts `<AiNpc />` with the Web Speech voice (zero-config) and a
sample VRM, deployed to a static host (Vercel/Netlify/GitHub Pages). Link it at the top of the root
README with a GIF.

> Stub for now — added once the avatar engine (Phase 2) renders.
